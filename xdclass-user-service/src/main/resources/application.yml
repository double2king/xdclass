server:
  port: 7071

spring:
  application:
    name: xdclass-user-service

#数据库配置
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/xdclass_user?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: password
  redis:
    host: 121.4.210.162
    port: 4001
    password: football@5453@1
    timeout: 3000ms

  mail:
    host: smtp.qq.com #发送邮件服务器
    username: 494480690@qq.com #发送邮件的邮箱地址
    password: wwgshctftgehbgbb #客户端授权码，不是邮箱密码,网易的是自己设置的
    from: 494480690@qq.com # 发送邮件的地址，和上面username一致
    properties.mail.smtp.starttls.enable: true
    properties.mail.smtp.starttls.required: true
    properties.mail.smtp.ssl.enable: true
    default-encoding: utf-8


#配置plus打印sql日志
mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl


#设置日志级别,ERROR/WARN/INFO/DEBUG,默认是INFO以上才显示
logging:
  level:
    root: INFO